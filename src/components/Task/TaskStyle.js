import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  taskCard: {
    // styles for task card
  },
  row: {
    // styles for row
  },
  isFinished: {
    // styles for finished status
  },
  name: {
    // styles for name text
  },
});
